-- Some plugins don't need own files because their configuration
-- is too small or non existent. Just put them here.
return {
	{ "tpope/vim-sleuth" }, -- Automatically detect shiftwidth and tabstop
}
